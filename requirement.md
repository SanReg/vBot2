Create a discord bot using node js, that should work across all servers with global registered slash commands

It uses /deposit and /withdraw commands to deposit and withdraw lightning amount
Use this docs as reference to implement:
https://coinos.io/docs